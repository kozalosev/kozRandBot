services:
  kozRandBot:
    build: .
    image: kozalo/kozrandbot
    container_name: kozrandbot
    working_dir: /home/kozRandBot
    restart: unless-stopped
    env_file:
      - path: .env
        required: false
    volumes:
    - "/tmp:/tmp"
    expose:
      - ${APP_PORT:-8080}
    logging:
      driver: local
